---
phase: "05"
plan: "02"
name: "Verification Pass — Full CI Gate Simulation"
type: autonomous
wave: 2
depends_on: ["05-01"]
requirements: [CI-01, CI-02, CI-03, CI-04, CI-05]
---

# Phase 05 Plan 02: Verification Pass

## Objective

Run all CI gates locally to confirm the workflow will pass in GitHub Actions.
Verify git is clean (no leaked artifacts).

## Tasks

### Task 1 — Full local CI gate simulation
type: auto

Run all CI commands in sequence:
1. `npm run typecheck` — TypeScript gate
2. `npm run lint` — ESLint gate
3. `npm test` — Unit + RLS contract tests
4. `npm run build` with placeholder Supabase env vars — Build gate

Document results. If any gate fails, auto-fix or document with justification.

### Task 2 — Git cleanliness check (CI-04)
type: auto

Verify:
- `git status --porcelain` shows no unexpected tracked-but-dirty files
- `git ls-files | Select-String "playwright-report|test-results|dist/"` returns nothing
- Confirm the new .gitignore exclusions are working

## Verification

All 4 gates exit 0 (or documented failures with explanation).
No CI artifacts are tracked by git.

## Success Criteria

Full gate simulation passes; git is clean; SUMMARY.md documents all results.
